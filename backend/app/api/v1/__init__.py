from fastapi import APIRouter

api_router = APIRouter()

# Register routes here as we build them
# from app.api.v1.endpoints import sessions
# api_router.include_router(sessions.router, prefix="/sessions", tags=["sessions"])
